/* global wpspAdmin */
( function () {
	'use strict';

	// Example: generic AJAX helper.
	// window.wpspAjax = function ( action, data ) {
	//     return fetch( wpspAdmin.ajaxUrl, {
	//         method  : 'POST',
	//         headers : { 'Content-Type': 'application/x-www-form-urlencoded' },
	//         body    : new URLSearchParams( { action, nonce: wpspAdmin.nonce, ...data } ),
	//     } ).then( r => r.json() );
	// };
} )();
